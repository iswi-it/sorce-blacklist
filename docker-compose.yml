version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - 8001:8000
    volumes:
      - ./backend:/app
    env_file:
      - ./backend.env

  frontend:
    build: ./frontend
    volumes:
      - './frontend:/app'
      - '/app/node_modules'
    ports:
      - 8081:80
